# 開発時の要件と検証

- 日本語で回答する。コミットメッセージは日本語の一言とし、AIの名前を含めない。
- 決定事項は個人メモリーではなく、このプロジェクトの文書とテストに残す。
- 本人による連携解除はDiscordの認証チャンネルで `/unlink` を実行して行う。対象は投稿者のDiscord IDから決め、他人を指定させない。
- 本人解除では認証情報の永続化、ホワイトリスト削除、接続中の本人の即時切断を行う。未連携と失敗を成功扱いしない。
- コマンド形式はユーザー指定のスラッシュコマンド。解除後は通常のコード認証で再連携可能とする。
- 完成条件は本人だけの解除、対象チャンネルの制限、未連携時の応答、保存と再読み込み、即時切断。`mvn test package` で回帰検証する。
